# 공개 저장소용. export 시 대상 디렉터리의 .gitignore 로 복사된다.
#
# private 저장소의 .gitignore 와 다르다. 그쪽은 개발 환경에 종속된 규칙을 담고 있어
# 공개 독자에게 무의미하다.

# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/
.venv/
venv/
env/

# ---------------------------------------------------------------------------
# 데이터셋 자동 다운로드 위치
# ---------------------------------------------------------------------------
data/

# ---------------------------------------------------------------------------
# 실험 산출물
#
# results/public/  논문의 통계를 재계산할 수 있는 행 단위 결과. **추적한다**
# results/raw/     전체 재현을 직접 돌릴 때 생기는 step 단위 로그. 대용량이며
#                  스크립트로 결정론적으로 재생성되므로 추적하지 않는다
# ---------------------------------------------------------------------------
results/raw/**
!results/raw/.gitkeep
results/checkpoints/**
!results/checkpoints/.gitkeep
runs/
tensorboard/
*.pt
*.pth
*.zip
*.log

# ---------------------------------------------------------------------------
# LaTeX 빌드 산출물
# ---------------------------------------------------------------------------
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.out
*.synctex.gz
*.toc
paper/main.pdf

# ---------------------------------------------------------------------------
# Notebook 실행 흔적
# ---------------------------------------------------------------------------
.ipynb_checkpoints/

# ---------------------------------------------------------------------------
# OS / 에디터
# ---------------------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
